# ===========================================
# TELNYX CONFIGURATION
# ===========================================
# These environment variables are required for Telnyx integration.
# Get these from https://portal.telnyx.com/

# API Key (Required)
# Found in: API Keys section of Telnyx Mission Control
# Format: KEY... (starts with KEY)
TELNYX_API_KEY=

# Connection ID (Required for Voice)
# This is your SIP Connection or Credential Connection ID
# Found in: Voice > SIP Connections or Voice > Credential Connections
# Used for: Making outbound calls via Call Control API
TELNYX_CONNECTION_ID=

# Messaging Profile ID (Required for SMS)
# Found in: Messaging > Messaging Profiles
# Used for: Sending SMS/MMS messages
TELNYX_MESSAGING_PROFILE_ID=

# Verification Number (Optional)
# A Telnyx number to use for sending verification codes
# Should be a number you own, in E.164 format: +15551234567
TELNYX_VERIFICATION_NUMBER=

# ===========================================
# WEBHOOK CONFIGURATION
# ===========================================
# Configure these URLs in Telnyx Portal for each service

# Voice Webhooks (Configure on your Connection)
# Voice Webhook URL: https://your-domain.com/api/voice/telnyx/webhook
# Voice Failover URL: https://your-domain.com/api/voice/telnyx/webhook (optional backup)

# SMS Webhooks (Configure on your Messaging Profile)
# Inbound Webhook URL: https://your-domain.com/api/sms/telnyx/receive
# Status Webhook URL: https://your-domain.com/api/sms/telnyx/status

# ===========================================
# TELNYX PORTAL SETUP CHECKLIST
# ===========================================

# 1. CREATE API KEY
#    - Go to: Auth > API Keys > Create API Key
#    - Copy the key and set TELNYX_API_KEY above

# 2. CREATE MESSAGING PROFILE (for SMS)
#    - Go to: Messaging > Messaging Profiles > Add new profile
#    - Name: "Call Helm Production" (or your choice)
#    - Enable API V2
#    - Set Webhook URL: https://your-domain.com/api/sms/telnyx/receive
#    - Set Status Webhook: https://your-domain.com/api/sms/telnyx/status
#    - Copy the Profile ID and set TELNYX_MESSAGING_PROFILE_ID above

# 3. CREATE CREDENTIAL CONNECTION (for Voice)
#    - Go to: Voice > Credential Connections > Add Connection
#    - Name: "Call Helm Voice"
#    - Set Webhook URL: https://your-domain.com/api/voice/telnyx/webhook
#    - Set Webhook API Version: 2
#    - Copy the Connection ID and set TELNYX_CONNECTION_ID above

# 4. PURCHASE PHONE NUMBERS
#    - Go to: Numbers > Search & Buy
#    - Purchase numbers with Voice + SMS capabilities
#    - Assign to your Messaging Profile
#    - Assign to your Credential Connection

# 5. CONFIGURE OUTBOUND VOICE PROFILE (Optional)
#    - Go to: Voice > Outbound Voice Profiles
#    - Create profile with allowed destinations (US, CA, etc.)
#    - Associate with your Credential Connection

# ===========================================
# RECORDING STORAGE (Optional)
# ===========================================
# Telnyx recording URLs expire after 10 minutes.
# You should download and store recordings permanently.
# Configure your own S3 bucket or use Telnyx's storage options.

# If using custom S3 for recordings:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# AWS_S3_RECORDINGS_BUCKET=your-recordings-bucket
